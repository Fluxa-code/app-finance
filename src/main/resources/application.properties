spring.application.name=finance
spring.jpa.hibernate.ddl-auto=validate

# Respostas de erro: nunca vazar stack trace (seguranca), mas mostrar a mensagem
server.error.include-stacktrace=never
server.error.include-message=always

# Segredo que ASSINA os tokens JWT. Quem tem o segredo consegue forjar tokens,
# entao: este valor e SO de desenvolvimento. Em producao vem da variavel de
# ambiente JWT_SECRET (o ${VAR:default} le a env var e cai no default se nao existir).
app.jwt.secret=${JWT_SECRET:2Sm/wNf9NTQ7yvboOVCVWLdhAGblkX0Ehbo4dRzmvo29CibECdknLBI8GipFpdDs}
